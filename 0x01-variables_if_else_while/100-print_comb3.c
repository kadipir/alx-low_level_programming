#include <stdio.h>
#include <stdlib.h>
/**
 * main - entry point
 * Return: 0 ()Success)
 */
int main(void)
{int x, y;
for (x = 0 ; x < 10 ; x++)
{for (y = 0 ; y < 10 ; y++)
if (i < j)
{{putchar(i);
putchar(j);
if (i !== 8 || i == 8 && j != 9)
{putchar(',');
putchar(' ');
}
}
}
}
putchar('\n');
return (0);
}
