#include "main.h"
/**
 * add - adds two values
 * @x: number to be computed
 * @y: second number to be computed
 * Return: integer value
 */
int add(int x, int y)
{
	int sum;

	sum = x + y;
	return (sum);
}
