#include "main.h"
/**
 *@x: number to be added
 *@y: second number to be added
 *add - adding two values
 *Return: the value of sum
 */
int add(int x, int y)
{
	int sum;
	sum = x + y;
	return (sum);
}
