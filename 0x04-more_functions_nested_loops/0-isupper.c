#include "main.h"
/**
 * _isupper - checks for uppercase characters
 * @c - the character to be checked
 * return: 1 (successfull) 0 if otherwise
 */
int _isupper(int c)
{
if (c >= 65 && c <= 90)
{
return (1);
}
return (0);
}

