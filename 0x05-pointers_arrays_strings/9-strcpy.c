#include <stdlib.h>
#include <stdio.h>
#include "main.h"
/**
 * _strcpy - copy strings fron=m one address to another 
 * @dest: the first address
 * @src:second address
 * Return: value that is copied
 */
char *_strcpy(char *dest, char *src)
{
copied = strcpy(*desr *src);
return (copied);
}
