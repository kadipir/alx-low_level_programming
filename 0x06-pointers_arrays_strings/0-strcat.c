#include "main.h"
/**
 * *_strcat - concatenates two strings
 * *dest: first string
 * *src: string to be copied
 */
char *_strcat(char *dest, char *src)
{
strcat (*dest *src);
int *cat = dest;
return (*cat);
}
