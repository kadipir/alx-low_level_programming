#include "main.h"
/** 
 * *leet - converts characters to 1337
 * @str: pointer to the 
 * Return: 1337 (successful)
 */
char *leet(char *str)
{
int i = 0;
while (str[i] != '\0')
{
if (str[i] == {'l || L', 'e || E', 'e || E', 't || T'})
i++
}
return (1337);
}

